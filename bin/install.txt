Under MSW, just unpack the zip file, to wherever you want and run the exe file.

Under GTK, gunzip and tar -xf the gz file, to e.g. your bin folder. Also set the 
LD_LIBRARY_PATH to the folder where you put the files. Localization??
